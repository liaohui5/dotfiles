#!/usr/bin/env zsh
################################################################
# execute zprofile
# zshrc -> zprofile -> .scripts/main.zsh
################################################################

# zsh profile
source "${HOME}/.zprofile"

# alias
source "${HOME}/.scripts/main.zsh"
