# 快速生成一些配置文件模板文件

function tpl() {
  template_name=$1
  if [[ $template_name == "prettier" ]]; then

  elif [[ $template_name == "gitignore" ]]; then

  elif [[ $template_name == "gitignore" ]]; then

  fi
}
